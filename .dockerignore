# Send only the files used by Dockerfile to the build daemon. This prevents
# credentials, APKs, emulator data, and local Git history from entering the
# build context even if a new local file is not covered by .gitignore.
**
!Dockerfile
!entrypoint.sh
!android-avd.conf
